<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.mapper.ContestParticipantMapper">

    <insert id="insert">
        insert into contest_participant(contest_id, user_id, registered_at, is_official, team_name)
        values
            (#{contestId}, #{userId}, #{registeredAt}, #{isOfficial}, #{teamName})
    </insert>

    <select id="findByContestAndUser" resultType="com.system.entity.ContestParticipant">
        select * from contest_participant where contest_id = #{contestId} and user_id = #{userId}
    </select>

    <select id="countByContestId" resultType="long">
        select count(user_id) from contest_participant where contest_id = #{contestId}
    </select>
</mapper>

