<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.mapper.ContestProblemMapper">

    <insert id="batchInsert">
        insert into contest_problem (contest_id, problem_id, display_order)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.contestId}, #{item.problemId}, #{item.displayOrder})
        </foreach>
    </insert>

    <delete id="deleteByContestId">
        delete from contest_problem where contest_id = #{contestId}
    </delete>

    <select id="findProblemsByContestId" resultType="com.system.vo.ContestDetailVO$ProblemInContestVO">
        select
            p.problem_id,
            p.title,
            cp.display_order,
            p.time_limit,
            p.memory_limit
        from problem p
                 join contest_problem cp on p.problem_id = cp.problem_id
        where cp.contest_id = #{contestId}
        order by cp.display_order asc
    </select>

    <select id="countByProblemId" resultType="java.lang.Integer">
        select count(contest_id) from contest_problem where problem_id = #{problemId}
    </select>

</mapper>

