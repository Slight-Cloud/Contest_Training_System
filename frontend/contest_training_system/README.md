# 🏆 竞赛集训系统 - 前端项目

基于 Vue 3 + TypeScript + Element Plus 构建的在线竞赛集训平台前端。

## ✨ 技术栈

- **框架**: Vue 3 (Composition API)
- **语言**: TypeScript
- **UI 组件**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **代码编辑器**: Monaco Editor
- **构建工具**: Vite
- **包管理器**: pnpm

## 🚀 快速开始

### 环境要求

- Node.js >= 16
- pnpm >= 8

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
pnpm dev
```

访问: http://localhost:9999

### 生产构建

```bash
pnpm build
```

构建产物在 `dist/` 目录

### 预览构建产物

```bash
pnpm preview
```

## 📁 项目结构

```
contest-system-frontend/
├── dist/                   # 构建产物
├── docs/                   # API 文档
├── public/                 # 公共静态资源
├── src/
│   ├── api/               # API 接口
│   ├── assets/            # 资源文件
│   ├── components/        # 公共组件
│   ├── layout/            # 布局组件
│   ├── router/            # 路由配置
│   ├── store/             # Pinia 状态管理
│   ├── types/             # TypeScript 类型定义
│   ├── utils/             # 工具函数
│   ├── views/             # 页面组件
│   ├── App.vue            # 根组件
│   ├── main.ts            # 入口文件
│   └── style.css          # 全局样式
├── index.html             # HTML 模板
├── package.json           # 依赖配置
├── vite.config.ts         # Vite 配置
├── tsconfig.json          # TypeScript 配置
├── nginx.conf             # Nginx 配置示例
└── DEPLOYMENT.md          # 部署指南

```

## 🎯 主要功能

### 角色系统
- **学生**: 参加竞赛、提交代码、查看排名
- **教师**: 创建题目、管理竞赛、查看学生表现
- **管理员**: 用户管理、系统配置

### 核心模块
- 🏠 **首页仪表盘** - 数据概览和快捷入口
- 📝 **题库管理** - 题目创建、编辑、测试数据管理
- 🏆 **竞赛系统** - 竞赛创建、参赛、实时排名
- 💻 **代码提交** - Monaco 编辑器、多语言支持
- 📊 **训练计划** - 制定和跟踪学习计划
- 👥 **用户管理** - 用户增删改查、权限管理

## 🔌 API 配置

后端 API 基础路径在 `src/utils/request.ts` 中配置:

```typescript
const service = axios.create({
    baseURL: '/api',  // 开发环境通过 Vite 代理
    timeout: 10000,
});
```

Vite 开发代理配置在 `vite.config.ts`:

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8080',  // 后端服务地址
    changeOrigin: true,
  }
}
```

## 📦 部署

详细部署步骤请查看: [DEPLOYMENT.md](./DEPLOYMENT.md)

### 快速部署

```bash
# 1. 构建
pnpm build

# 2. 将 dist 目录上传到服务器
# 3. 配置 Nginx (参考 nginx.conf)
# 4. 启动服务
```

## 🛠️ 开发规范

### 代码风格
- 使用 TypeScript 严格模式
- 组件使用 Composition API + `<script setup>`
- 统一使用 camelCase 命名

### Git 提交规范
```
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
perf: 性能优化
test: 测试相关
chore: 构建/工具相关
```

## 📄 许可

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request!
